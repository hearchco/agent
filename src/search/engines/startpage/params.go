package startpage

const (
	// Variable params.
	paramQueryK = "q"
	paramPageK  = "page"

	// Constant params.
	paramSafeSearchK, paramSafeSearchV = "qadf", "none" // Can be "none" or empty param (empty means it's enabled).
)
